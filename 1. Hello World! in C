#include <stdio.h>
#include <string.h>

int main() {
    char s[100];
    // Scanset [^\n] reads everything until a newline; %*c discards the newline
    scanf("%[^\n]%*c", s);
    
    printf("Hello, World!\n");
    printf("%s\n", s);
    
    return 0;
}
